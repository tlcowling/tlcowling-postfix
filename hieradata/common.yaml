---
postfix:
  install:
    version      : '2.9.6-2'
    manage_user  : false
    postfix_user : 'postfix'
  config:
    soft_bounce: 'no'
    queue_directory: '/var/spool/postfix'
    mydestination:
      - $myhostname
      - localhost.$mydomain
      - locahost
      - anotherone
